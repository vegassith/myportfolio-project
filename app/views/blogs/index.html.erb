<%= render 'featured_blogs', collection: @blogs.limit(2)%>
<div class="col-md-8 blog-main">
    <%= render @blogs %>
    <nav class="blog-pagination">
            <%= paginate @blogs %>
    </nav>
</div><!-- /.blog-main -->
